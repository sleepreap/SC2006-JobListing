const OneMapRouting = () => {
  //     const data = JSON.stringify(false);

  // const xhr = new XMLHttpRequest();

  // xhr.addEventListener("readystatechange", function () {
  //         if (this.readyState === this.DONE) {
  //         }
  //       });

  // xhr.open("GET", "https://www.onemap.gov.sg/api/public/routingsvc/route?start=1.319728%2C103.8421&end=1.319728905%2C103.8421581&routeType=walk");

  // xhr.setRequestHeader("Authorization", "**********************");

  // xhr.send(data);
  return <h2>Return JSON!!!!</h2>;
};

export default OneMapRouting;
